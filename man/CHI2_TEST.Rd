% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-chi2.R
\name{CHI2_TEST}
\alias{CHI2_TEST}
\title{Chi-Square Test}
\usage{
CHI2_TEST(
  .model = NULL,
  .data = NULL,
  .correct = TRUE,
  .p = NULL,
  .rescale_p = FALSE,
  .simulate_p_value = NULL,
  ...
)
}
\arguments{
\item{.model}{A \code{cont_tab()} model ID object. When supplied, the test
executes immediately. When \code{NULL} (default), returns a \code{test_spec} for
use in the lazy pipeline via \code{\link[=prepare_test]{prepare_test()}}.}

\item{.data}{A data frame. Only used when \code{.model} is supplied directly
(standalone path). Ignored in the pipeline path.}

\item{.correct}{Logical. Whether to apply Yates' continuity correction for
2x2 tables. Defaults to \code{TRUE}.}

\item{.p}{A numeric vector of probabilities representing the expected
proportions under the null hypothesis. When \code{NULL} (default), equal
probabilities are assumed (goodness of fit against a uniform
distribution).}

\item{.rescale_p}{Logical. Whether to rescale \code{.p} to sum to 1. Defaults
to \code{FALSE}.}

\item{.simulate_p_value}{A list from \code{opts_simul_chi()} controlling Monte
Carlo simulation of the p-value. Defaults to \code{NULL} (no simulation).}

\item{...}{Additional arguments passed to methods.}
}
\value{
An \code{htest_infer} object of subclass \code{chi2test} (standalone), or a
\code{test_spec} object (pipeline).
}
\description{
\code{CHI2_TEST()} performs a chi-square test of independence or goodness of fit.
It dispatches on the \code{cont_tab()} model ID declared in \code{\link[=define_model]{define_model()}}.
}
\section{Supported model IDs}{

\itemize{
\item \code{cont_tab()} — chi-square test of independence between two categorical
variables, or goodness of fit when \code{.p} is supplied
}
}

\section{Simulation}{

When exact computation is not feasible, Monte Carlo simulation of the
p-value can be enabled via \code{opts_simul_chi()} passed to \code{.simulate_p_value}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{mtcars |>
    define_model(cont_tab(vs, am)) |>
    prepare_test(CHI2_TEST) |>
    update(.simulate_p_value = opts_simul_chi(simul = TRUE, B = 5000)) |>
    run_test()
}\if{html}{\out{</div>}}
}

\examples{
mtcars |>
    define_model(cont_tab(vs, am)) |>
    prepare_test(CHI2_TEST) |>
    run_test()

mtcars |>
    define_model(cont_tab(vs, am)) |>
    run_test(CHI2_TEST)

# With simulation
mtcars |>
    define_model(cont_tab(vs, am)) |>
    prepare_test(CHI2_TEST) |>
    update(.simulate_p_value = opts_simul_chi(simul = TRUE, B = 5000)) |>
    run_test()

}
\seealso{
\code{\link[=define_model]{define_model()}}, \code{\link[=cont_tab]{cont_tab()}}, \code{\link[=prepare_test]{prepare_test()}}, \code{\link[=run_test]{run_test()}},
\code{\link[=opts_simul_chi]{opts_simul_chi()}}, \code{\link[stats:chisq.test]{stats::chisq.test()}}
}
