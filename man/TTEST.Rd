% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-ttest.R
\name{TTEST}
\alias{TTEST}
\title{T-Test}
\usage{
TTEST(
  .model = NULL,
  .data = NULL,
  .name = "T-Test",
  .paired = TRUE,
  .mu = 0,
  .alt = "two.sided",
  .ci = 0.95,
  ...
)
}
\arguments{
\item{.model}{A model ID object from \code{\link[=rel]{rel()}}, \code{\link[=one_sample]{one_sample()}}, \code{\link[=pairwise]{pairwise()}},
or a formula. When supplied, the test executes immediately. When \code{NULL}
(default), returns a \code{test_spec} for use in the lazy pipeline via
\code{\link[=prepare_test]{prepare_test()}}.}

\item{.data}{A data frame. Only used when \code{.model} is supplied directly
(standalone path). Ignored in the pipeline path.}

\item{.name}{A string used as the test title in output. Defaults to
\code{"T-Test"}.}

\item{.paired}{Logical. Whether to perform a paired t-test. Defaults to
\code{TRUE}.}

\item{.mu}{Numeric. The reference value under the null hypothesis. Defaults
to \code{0}.}

\item{.alt}{A string specifying the alternative hypothesis. One of
\code{"two.sided"} (default), \code{"greater"}, or \code{"less"}.}

\item{.ci}{Numeric. The confidence level for the confidence interval.
Defaults to \code{0.95}.}

\item{...}{Additional arguments passed to methods.}
}
\value{
An \code{htest_infer} object (standalone), or a \code{test_spec} object
(pipeline). The specific subclass depends on the model ID:
\itemize{
\item \code{ttest_two} for \code{rel()}
\item \code{ttest_one} for \code{one_sample()}
\item \code{ttest_pairwise} for \code{pairwise()}
\item \code{ttest_formula} for formula
\item \code{ttest_permute} for permutation resampling
\item \code{ttest_boot} for bootstrap resampling
}
}
\description{
\code{TTEST()} performs a t-test for one-sample, two-sample, paired, pairwise,
or formula-based comparisons. It dispatches on the \code{model_id} type declared
in \code{\link[=define_model]{define_model()}}, automatically selecting the appropriate variant.
}
\section{Supported model IDs}{

\itemize{
\item \code{compare_by()}: targeted for two-sample or paired t-test, comparing \code{x} across (2)
groups
\item \code{one_sample()}: one-sample t-test against a reference value \code{.mu}
\item \code{pairwise()}: all pairwise t-tests across selected variables
\item \code{formula}: passed directly to \code{\link[stats:t.test]{stats::t.test()}}
}
}

\section{Resampling}{

\code{TTEST()} supports permutation and bootstrap resampling via \code{\link[=resample]{resample()}}
for \code{rel()} models. Use \code{.method = "permute"} for permutation testing or
\code{.method = "boot"} for bootstrap confidence intervals.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{sleep |>
    define_model(rel(group, extra)) |>
    prepare_test(TTEST) |>
    resample(.method = "permute", n = 5000) |>
    run_test()
}\if{html}{\out{</div>}}
}

\examples{
# Pipeline path (lazy)
sleep |>
    define_model(rel(group, extra)) |>
    prepare_test(TTEST) |>
    update(.paired = FALSE) |>
    run_test()

# Eager path (immediate)
sleep |>
    define_model(rel(group, extra)) |>
    run_test(TTEST)

# Standalone path
TTEST(rel(group, extra), sleep)

# One-sample
sleep |>
    define_model(one_sample(extra)) |>
    run_test(TTEST)

# Pairwise
sleep |>
    define_model(pairwise(extra, group)) |>
    run_test(TTEST)

# Permutation resampling
sleep |>
    define_model(rel(group, extra)) |>
    prepare_test(TTEST) |>
    resample(.method = "permute", n = 5000) |>
    run_test()

}
\seealso{
\code{\link[=define_model]{define_model()}}, \code{\link[=prepare_test]{prepare_test()}}, \code{\link[=run_test]{run_test()}}, \code{\link[=resample]{resample()}},
\code{\link[stats:t.test]{stats::t.test()}}
}
